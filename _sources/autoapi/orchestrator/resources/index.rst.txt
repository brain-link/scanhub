orchestrator.resources
======================

.. py:module:: orchestrator.resources

.. autoapi-nested-parse::

   Definition of dagster data lake resource for acquisition data.



Classes
-------

.. autoapisummary::

   orchestrator.resources.DataLakeResource


Module Contents
---------------

.. py:class:: DataLakeResource

   Bases: :py:obj:`dagster.ConfigurableResource`


   Dagster data lake resource.


   .. py:method:: get_mrd_path(task_dir: str) -> pathlib.Path

      Find the MRD file inside a task directory.

      Parameters
      ----------
      task_dir
          Absolute path to the task data directory.

      Returns
      -------
      Path
          Path to the acquisition ISMRMRD file.



   .. py:method:: get_device_parameter(task_dir: str) -> tuple[str, dict]

      Read device parameters from device_parameter.json inside a task directory.

      Parameters
      ----------
      task_dir
          Absolute path to the task data directory.

      Returns
      -------
      tuple[str, dict]
          Device ID and parameter dictionary.



